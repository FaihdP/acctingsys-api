enum RESPONSE_MESSAGES {
  DOCUMENT_SAVED = 'Document saved successfully.',
  INVALID_OR_MISSING_FIELD = 'Invalid or missing fields: ',
  INTERNAL_SERVER_ERROR = 'Internal server error',
}

export default RESPONSE_MESSAGES;